/target
**/*.rs.bk
*.pdb

# Harness-local configuration (personal settings, absolute paths) — ignored
# by default; share one only via an explicit `!` whitelist (CONTRIBUTING.md).
# Config templates live in docs/setup.md.
.mcp.json
.claude/
.codex/
.grok/
